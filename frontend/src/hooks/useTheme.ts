/**
 * @deprecated Use useThemeContext from '../contexts' instead
 * This hook is kept for backwards compatibility
 */
export { useThemeContext as useTheme } from '../contexts';
export type { ThemeMode, EffectiveTheme } from '../contexts';
